# TX_TRAIN_2019_2020

Training-only bootstrap archive layout (§2.1, §7).

- Label: `training-only`
- Intended contents: hub-level DA/RT SPP for 2019–2020
- Retirement: supplements campaign forecasts only while completed campaign days are fewer than 90, then the bootstrap label comes off
- This folder does **not** ship SOURCE prices. `rows` is empty and `source_batch_count` is 0.

Do not use this archive as a campaign price path. Campaign settlement coverage is `TX_GAME_ENERGY_V1`.
