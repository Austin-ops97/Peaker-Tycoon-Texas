# Pipeline fixtures

Fixtures exercise fetch refusal, SHA-256 retention, validation, and the refusal to publish a complete SOURCE pack.

They are labeled `fixture_not_source`. A fixture decimal is not an ERCOT settlement price. Publication timestamps in fixtures stay null.

The golden gates do not read this directory. They read `Data/packs/TX_GAME_ENERGY_V1/coverage-manifest.json`.
